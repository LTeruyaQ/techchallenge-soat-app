# Este arquivo foi limpo.
# As fontes de dados aws_iam_role foram removidas para evitar
# erros de permissão iam:GetRole no ambiente restrito do AWS Academy.
# Os ARNs das roles agora são construídos manualmente no locals.tf.
